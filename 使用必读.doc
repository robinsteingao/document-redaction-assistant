<html>
<head><meta charset="utf-8"><title>文档安全脱敏助手 M24.2 使用必读</title></head>
<body>
<h1>文档安全脱敏助手 M24.2 使用必读</h1>
<p><b>先看结论：</b>本工具在本机离线处理文件，原始文件不上传；普通用户优先双击 <code>app\start_desktop_app.bat</code> 打开界面；<code>local_mapping.private.enc</code> 是本地加密映射表，只留在本机，不要上传或外发；请牢记加密口令，丢失后无法还原；复核页可直接点选字段处理方式。</p>
<h2>首次部署流程</h2>
<ol>
<li>解压到短路径，例如 <code>D:\DRA</code> 或 <code>C:\DRA</code>。</li>
<li>如包内提供 <code>app\install_offline_ocr.bat</code>，先双击安装 OCR 依赖。</li>
<li>如包内提供 <code>app\validate_offline_ocr.bat</code>，再双击校验 OCR。</li>
<li>双击 <code>app\start_desktop_app.bat</code>，等待浏览器自动打开。</li>
<li>浏览器未自动打开时，手动访问 <code>http://127.0.0.1:8765</code>。</li>
</ol>
<h2>日常操作方法</h2>
<ol>
<li>填写项目代号，并设置“本地映射表加密口令”。</li>
<li>填写待处理文件或文件夹路径；每行一个文件，也可以填写文件夹做批量脱敏。</li>
<li>点击“预检文件”，重点看可直接处理、需先转换、跳过、暂不支持、路径不存在和推荐 OCR 模式。</li>
<li>点击“开始生成脱敏结果包”，等待处理状态完成。</li>
<li>打开输出目录，查看 <code>redaction_upload_package.json</code>、<code>sandbox_import_package.json</code>、<code>redaction_review_report.md</code>、<code>review_workspace.html</code> 和 <code>local_mapping.private.enc</code>。</li>
<li>遇到问题时点击“生成技术支持包”，复制生成的 JSON 给技术支持；技术支持包不包含原文、本地加密映射表、加密口令或密钥。</li>
</ol>
<h2>复核选择文件怎么打开</h2>
<p>打开输出目录中的 <code>review_workspace.html</code>，可在页面表格里直接选择“保留原样、遮盖隐藏、替换为假名、保留区间”，再导出 <code>review_decisions.json</code>。如需查看 JSON，可用“记事本”、Notepad++、VS Code 打开。</p>
<h2>常见报错处理</h2>
<ul>
<li>“未配置 OCR 引擎”：先运行 <code>app\install_offline_ocr.bat</code>，再运行 <code>app\validate_offline_ocr.bat</code>。非扫描件通常不受影响。</li>
<li>“the application cant be start,user installation couldn't be completed”：关闭窗口后重新运行 <code>app\start_desktop_app.bat</code>，确认解压路径较短且不在网络盘；仍失败时以管理员身份运行并联系技术支持。</li>
<li>“Failed to fetch / 本地服务未连接”：确认启动窗口未关闭，手动访问 <code>http://127.0.0.1:8765</code>，或运行 <code>app\start_local_service.bat</code> 后刷新。</li>
</ul>
<h2>批量脱敏</h2>
<p>可直接填写文件夹路径进行批量处理。多个项目建议分项目分别运行，避免不同项目的本地映射表混在一起。</p>
<h2>边界提醒</h2>
<p>“评价影响提醒”只提示脱敏选择是否可能影响后续评价，不等于无隐私残留，也不等于 STPE-AI 最终评价一定不受影响。</p>
<p>本地服务只面向本机浏览器页面使用，会限制 localhost 来源或包内 file:// 页面来源、请求签名和危险路径；它不是对外网络服务，也不能替代企业终端安全、法务审查或涉密审查。</p>
<p>身份证、银行卡、护照号、统一社会信用代码、地址等规则用于生成候选字段，仍可能漏报或误报；外发或上传前必须人工复核。</p>
<p>授权文件和试用计数包含本地签名校验，用于降低普通篡改风险；客户包不包含授权签发私钥，正式授权仍以发行方离线签发并发放的 license.json 为准。</p>
<p>本地映射表使用标准加密封装并兼容旧版解密，但实际安全性仍依赖客户设置的口令强度和本机安全环境。</p>
<h2>M25 愿景说明</h2>
<p>PaddleOCR 3.7 / PP-OCRv6、OCR 置信度复核、表格/Excel/WPS 单元格级脱敏和扫描 PDF 版面结构化解析属于 M25 愿景，不属于 M24.2 当前交付范围。</p>
</body>
</html>
