# 文档安全脱敏助手 — 盲测快速上手说明

> **适用版本**: 0.24.0-m24  
> **适用对象**: 不了解项目源码结构的测试人员  
> **本文件是唯一入口**: 按顺序执行即可完成盲测，无需阅读其他文档

---

## 0. 这是什么？

本包是一个**完全离线运行**的文档安全脱敏工具。它在您的电脑上本地完成以下工作：

- 解析项目申报材料（Word / Excel / PDF / TXT）
- 自动识别敏感字段（姓名、电话、邮箱、专利号、金额等）
- 按默认规则脱敏，生成脱敏上传包
- 检测脱敏是否会影响 STPE-AI 对项目的评价
- 生成复核工作区，供您逐项确认

**关键安全边界**：所有处理在您本地完成，原始文件不会离开您的电脑。

---

## 1. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64 位） |
| Python | **无需安装**（已内置） |
| Office | **无需安装**（已内置 LibreOffice 转换器） |
| OCR 引擎 | **已内置依赖包**，需运行一次安装脚本（约 2 分钟） |
| 磁盘空间 | 建议预留 2 GB |
| 网络 | **完全离线**，无需联网 |

---

## 2. 解压（重要！）

**必须解压到短路径**，例如：

```
D:\DRA
C:\DRA
D:\脱敏助手
```

**不要**解压到：

- 桌面深层目录（如 `C:\Users\张三\Desktop\文档安全脱敏助手...`）
- 含空格或特殊字符的路径
- 网络映射驱动器

解压后目录结构：

```
D:\DRA\
├── START_HERE.md          ← 包内入口（参考用）
├── BLIND_TEST_QUICK_START.md  ← 本文件（盲测入口）
├── setup.bat
├── app\
│   ├── start_desktop_app.bat  ← 主要启动入口
│   ├── install_offline_ocr.bat
│   ├── validate_offline_ocr.bat
│   ├── run_sample_self_test.bat
│   └── run_acceptance_smoke.bat
├── sample_data\
└── docs\
```

---

## 3. 首次启动（3 步完成）

### 步骤 1：安装 OCR 引擎依赖（约 2 分钟，仅首次需要）

双击运行：

```
app\install_offline_ocr.bat
```

**预期输出**：命令行窗口闪过后自动关闭，无红色错误信息。

如果提示 `OCR wheelhouse not found`，说明包不完整，请联系发包人。

### 步骤 2：校验 OCR 安装

双击运行：

```
app\validate_offline_ocr.bat
```

**预期输出**：显示 JSON 信息，其中 `"status": "enabled"` 表示 OCR 已就绪。

如果 `"status": "not_installed"`，请重新执行步骤 1。

### 步骤 3：启动产品界面

双击运行：

```
app\start_desktop_app.bat
```

**预期行为**：
1. 命令行窗口显示启动日志
2. 自动打开浏览器，显示产品操作界面
3. 界面标题为"文档安全脱敏助手"

如果浏览器未自动打开，手动访问 `http://127.0.0.1:8765`。

---

## 4. 使用流程

### 4.1 准备测试材料

在任意目录下准备测试文件，例如：

```
D:\测试材料\项目A申报书.docx
D:\测试材料\合同金额说明.pdf
D:\测试材料\技术指标.txt
```

### 4.2 在界面中操作

1. **填写项目代号**：输入一个标识符，如 `test_project_01`
2. **填写文件路径**：输入文件或文件夹的完整路径
   - 单个文件：`D:\测试材料\项目A申报书.docx`
   - 文件夹：`D:\测试材料`
3. **点击"预检文件"**：查看哪些文件可处理、哪些需要转换
4. **点击"开始生成脱敏包"**：等待处理完成
5. **查看结果**：处理完成后，输出目录会生成以下文件

### 4.3 输出文件说明

| 文件名 | 说明 |
|--------|------|
| `redaction_upload_package.json` | **脱敏上传包**（可发送给 STPE-AI） |
| `local_mapping.private.json` | **本地映射表**（留在您电脑，用于还原） |
| `redaction_review_report.md` | **脱敏审查报告**（含评价影响门禁结果） |
| `review_workspace.html` | **复核工作区**（浏览器打开，逐项确认） |
| `sandbox_import_package.json` | **沙箱导入包**（用于 STPE-AI 导入） |

---

## 5. 评价影响门禁（M24 新功能）

门禁回答的问题是：**"客户的脱敏选择，会不会削弱 STPE-AI 对该项目的评价？"**

### 5.1 门禁等级

| 等级 | 含义 | 您需要做什么 |
|------|------|-------------|
| `pass` | 未发现会影响评价的强脱敏 | 可以继续 |
| `warning` | 有字段被保留（未脱敏），可能影响评价 | 查看报告，确认是否可接受 |
| `blocked_requires_confirmation` | 客户强制脱敏了技术指标等关键字段，**会阻断评价** | 必须显式确认接受风险后才能继续 |

### 5.2 如何查看门禁结果

打开 `redaction_review_report.md`，搜索"评价影响门禁"，查看 `overall_level`。

### 5.3 客户自定义脱敏决策

如果您需要修改默认脱敏策略：

1. 打开 `review_workspace.html`
2. 查看各字段的默认脱敏建议
3. 导出 `review_decisions.json`
4. 修改决策（将 `action` 改为 `keep` 或 `redact`，选择策略 `pseudonym` / `range` / `mask`）
5. 将修改后的 JSON 粘贴到界面的"客户自定义脱敏决策 JSON"输入框
6. 重新生成脱敏包

**重要提示**：
- 仅提交决策不会标记 `customer_confirmed=true`
- 必须显式勾选"确认接受评价降级风险"才会记录确认
- 强制脱敏技术指标且未确认风险 → 门禁等级为 `blocked_requires_confirmation`

---

## 6. 快速验收（可选）

如果需要运行自动验收脚本：

```
app\run_sample_self_test.bat
```

**预期输出**：`SAMPLE_SELF_TEST_OK`

完整验收：

```
app\run_acceptance_smoke.bat
```

---

## 7. 已知限制

1. **OCR 准确率有限**：扫描件（图片型 PDF）的 OCR 识别可能不完美，低置信结果会标记为需人工复核
2. **金额区间换算**：英文金额的区间换算可能与直觉有偏差，这是辅助估计，不作判定依据
3. **原生文件选择器**：当前文件路径需手动输入，尚不支持系统文件选择对话框
4. **`.xls/.wps` 格式**：旧版 Office 格式先尝试本地转换，转换失败的文件不会进入上传包
5. **门禁是建议级**：门禁 `pass` 不等于"无隐私残留"，仅表示在已识别字段中未发现会阻断评价的强脱敏

---

## 8. 常见问题

| 问题 | 解决方案 |
|------|---------|
| 提示 `Cannot find Python` | 确认解压路径不含空格和中文，重新解压到短路径 |
| 提示 `DESKTOP_APP_START_FAILED` | 检查端口 8765 是否被占用，关闭其他服务后重试 |
| OCR 安装失败 | 确认已运行 `install_offline_ocr.bat`，检查磁盘空间 |
| 浏览器未自动打开 | 手动访问 `http://127.0.0.1:8765` |
| 处理大文件很慢 | 正常现象，PDF 和大型 Word 文件需要更多处理时间 |
| 输出目录找不到 | 默认输出到 `文档\文档安全脱敏助手输出\`，检查用户文档目录 |

---

## 9. 测试记录模板

每条测试用例请记录：

```
测试编号: §X.X
输入文件: [文件名/文本片段]
使用的 review_decisions.json: [有/无]
门禁结果:
  - overall_level: [pass/warning/blocked_requires_confirmation]
  - trl_impact: [none/low/medium/high]
  - benefit_impact: [none/low/medium/high]
  - customer_confirmed: [true/false]
脱敏正文片段: [粘贴关键段落]
是否与预期一致: [是/否]
备注: [任何异常现象]
```

---

## 10. 测试结论红线

1. 门禁 `pass` **不能**向客户承诺"无隐私残留"或"评价不受影响"
2. 本工具**不代表** STPE-AI 主系统评价规则、TRL 口径或 readiness 分值发生变化
3. 门禁映射为**启发式**，未经真实项目真值校准；内测仅用于功能验证
4. `.xls/.wps` 真实样本专项验证尚未完成，相关结论暂不外推

---

## 11. 技术支持

遇到问题请提供以下信息：

1. 解压路径
2. 运行的脚本名称
3. 完整错误输出（截图或复制文本）
4. `generated\` 目录下的日志文件（如有）

---

_本文件随盲测包一同分发，测试人员只需阅读本文件即可完成盲测。_
